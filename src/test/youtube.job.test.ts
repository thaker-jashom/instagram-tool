import dotenv from 'dotenv';
dotenv.config();

// NOTE: Queue functionality removed - Redis not required by HR document
console.log('YouTube discovery queue test disabled - queues removed');
process.exit(0);